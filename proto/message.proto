syntax = "proto3";

package messages;

message JoinMessage {
    string player_id = 1;
}

message WelcomeMessage {
    uint32 thing_id = 1;
}

message Message {
    oneof variant {
        JoinMessage join = 1;
        WelcomeMessage welcome = 2;
    }
}