{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile proto",
            "type": "shell",
            "command": "protoc proto/message.proto --rust_out=proto_rust"
        },
        {
            "type": "cargo",
            "command": "run",
            "problemMatcher": [
                "$rustc"
            ],
            "label": "rust: cargo run"
        }
    ]
}